import time
# import numpy as np

# a = 100000000
# array = [i for i in range(a)]

# start = time.time()
# values = np.array(array)
# search_value = a - 1
# results = np.where(values == search_value)[0]
# end = time.time() - start

# print(results, end)

# start = time.time()
# store = []
# for i in range(a):
#   if i == a - 1:
#     store.append(i)
# end = time.time() - start
# print(store, end)